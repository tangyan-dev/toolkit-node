module.exports = {
    del: require('./libs/del'),
    diff: require('./libs/diff'),
    file: require('./libs/file'),
    md5: require('./libs/md5'),
    mkdir: require('./libs/mkdir'),
    shell: require('./libs/shell'),
    zip: require('./libs/zip'),
    unzip: require('./libs/unzip'),
}